import { TagrankingPipe } from './tagranking.pipe';

describe('TagrankingPipe', () => {
  it('create an instance', () => {
    const pipe = new TagrankingPipe();
    expect(pipe).toBeTruthy();
  });
});
